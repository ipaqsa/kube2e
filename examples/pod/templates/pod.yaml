apiVersion: v1
kind: Pod
metadata:
  name: {{ .name }}
spec:
  restartPolicy: Never
  containers:
    - name: runner
      image: busybox:1.36
      command:
        - sh
        - -c
        - {{ .command | default "echo 'done'" | quote }}
