apiVersion: v1
kind: Service
metadata:
  name: {{ .name }}
  labels:
    app: {{ .app | default "webapp" }}
spec:
  # Selects the Deployment's pods by the shared app label.
  selector:
    app: {{ .app | default "webapp" }}
  ports:
    - name: http
      port: 80
      targetPort: 80
